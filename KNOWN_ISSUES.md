# KNOWN_ISSUES

> 记录已知问题、当前是否处理、延后原因和计划版本。
> 避免同一个问题被重复发现和讨论。实时更新。

---

## 🔴 需要修复

### K-001：历史文件被测试运行污染

**描述**：集成测试每次运行都会在 `~/.file-organizer/history.json` 中创建会话。多次运行后历史文件可能达到 1000 条上限，触发清理逻辑。

**影响**：V0.3.3 之前 `saveHistory` 用 `shift()` 删最新记录（实际是数组末尾），导致新会话被立即清除，undo 找不到会话。

**当前状态**：已修复（V0.3.3，commit b06f325）。`shift()` → `pop()`。

**根因**：历史数组 `unshift` 最新在前，`shift()` 删头部=删最新，`pop()` 删尾部=删最旧。

**计划版本**：V0.3.3 ✅

**后续建议**：测试运行前自动清空历史文件，或测试使用独立的历史存储路径。

---

### K-002：P1-6 接线回归测试部分实现

**描述**：UI → API → Server → Engine → Job → UI 字段连续性没有完整的自动化回归测试。V0.3.4 新增了场景测试覆盖用户操作路径，但 API 层面的字段透传测试（如 `sessionId` / `planId` / `confidence` 字段在各层间正确传递）仍未自动化。

**影响**：字段断裂（如字段重命名、类型变更）只能靠场景测试和人工 Review 发现。

**当前状态**：部分处理。V0.3.4 新增 `test/scenario.js`（29/29 通过），覆盖 Edit→Execute / Exclude→Execute / Restore→Execute / Plan Without ScanId / Foreign File Injection / Session Lifecycle / Classify Cancel / Undo Conflict。

**计划版本**：V0.3.4 部分完成，完整字段透传测试留到 V0.3.4 Review 后。

**建议方案**：对每个 API 端点写一个"字段透传"测试——构造输入 → 调用 API → 验证返回值中关键字段存在且类型正确。

---

### K-003：GitHub Actions 自动化验收未配置

**描述**：没有 CI/CD 流程。每次代码提交后需要手动跑测试，无法在 PR 阶段自动拦截回归。

**影响**：依赖人工记得跑测试；合并冲突可能引入未测试的代码。

**当前状态**：已处理。V0.3.4 新增 `.github/workflows/test.yml`，V0.3.5.1 新增 `test/run-with-server.js` 统一管理 server 生命周期，CI 可直接运行 `npm test`。

**计划版本**：V0.3.4 ✅ / V0.3.5.1 ✅

---

### K-015：测试服务器生命周期管理

**描述**：集成测试和场景测试依赖手动启动的服务器进程。`test:integration` 和 `test:scenario` 脚本假设服务器已在运行，没有内置的启动/停止逻辑。

**影响**：测试步骤多（先启动服务器 → 再跑测试 → 再停止服务器），容易遗忘导致测试失败。

**当前状态**：已修复。V0.3.5.1 新增 `test/run-with-server.js`，统一管理 server 生命周期（spawn → wait ready → run tests → kill）。`npm test` 可在干净环境运行。

**计划版本**：V0.3.5.1 ✅

---

## 🟡 已知限制

### K-004：macOS 文件夹选择仅限 Mac

**描述**：文件夹选择对话框依赖 macOS `osascript`，Windows / Linux 回退为手动路径输入。

**影响**：Windows / Linux 用户体验不完整，无法使用原生文件夹选择器。

**当前状态**：已知限制，非 Bug。

**计划版本**：V0.5（跨平台文件夹选择）。

---

### K-005：Job 状态不持久化

**描述**：Job 数据存储在服务端内存 Map 中，服务器重启后丢失。Job 创建后 60 秒自动清理（completed/failed/partial）或 30 秒（异常）。

**影响**：服务器重启后，进行中的 Job 无法恢复；客户端刷新页面后需要重新创建 Job。

**当前状态**：已知限制，设计如此。

**计划版本**：V1.0 评估（Job 持久化到文件）。

---

### K-006：历史记录清理策略是 FIFO 而非 LRU

**描述**：历史记录达到 1000 条移动操作上限时，从数组末尾（最旧会话）开始删除。但如果一个旧会话有 500 条移动，它被删除后总条目骤降，新会话存储空间不均衡。

**影响**：极端情况下，一个大会话可能占用一半配额，导致其他会话被提前清理。

**当前状态**：已知限制，V0.3.3 已修复清理方向（shift→pop）。

**计划版本**：V0.4 评估（改为按会话条目数加权清理，或增加配置项）。

---

### K-007：无 Lint / Type Check 工具链

**描述**：项目没有 ESLint、Prettier、TypeScript 等工程工具。代码风格和类型安全依赖人工 Review。

**影响**：潜在的风格不一致、隐式类型错误只能在运行时发现。

**当前状态**：已知限制。

**计划版本**：V0.4 评估（引入 ESLint + Prettier，TypeScript 可延后到 V1.0）。

---

## 🟢 已确认延后

### K-008：批量操作（反选 / 恢复排除）

**描述**：Review Queue 中需要支持批量反选、批量恢复排除、批量改目标目录。

**当前状态**：未开始。

**延后原因**：V0.3.x 系列聚焦基础设施，批量操作是体验优化，不影响核心流程。

**计划版本**：V0.4。

---

### K-009：重复文件检测

**描述**：扫描时识别内容相同的文件（通过哈希），提示用户是否合并或删除。

**当前状态**：未开始。

**延后原因**：需要读取文件内容计算哈希，大文件夹性能影响需要评估。且涉及删除操作，安全边界需要重新设计。

**计划版本**：V0.4。

---

### K-010：规则自定义

**描述**：允许用户创建自定义分类规则（如"所有 .txt 文件放到 文档/笔记"），优先级高于 AI 推断。

**当前状态**：未开始。

**延后原因**：需要设计规则引擎和 UI，属于 V0.4 功能扩展范畴。

**计划版本**：V0.4。

---

### K-011：跨平台文件夹选择

**描述**：Windows / Linux 原生文件夹选择器。

**当前状态**：未开始。

**延后原因**：需要 Electron 或系统级 API 调用，V0.5 路线图。

**计划版本**：V0.5。

---

### K-012：Electron 桌面封装

**描述**：将当前 HTTP 服务器 + 浏览器封装为桌面应用。

**当前状态**：未开始。

**延后原因**：V1.0 生产就绪目标。当前阶段保持纯 Web 应用，降低复杂度。

**计划版本**：V1.0。

---

### K-013：Content Extractor

**描述**：PDF / DOCX / PPTX / XLSX 等格式的内容提取和理解。

**当前状态**：未开始。

**延后原因**：需要引入文件解析库（如 `pdf-parse`、`mammoth`），与零依赖原则冲突。需要单独评估。

**计划版本**：V0.5 或 V1.0（取决于依赖评估结果）。

---

### K-014：AI 调用降级和超时策略

**描述**：当前 AI 批处理（20 文件/批）没有统一的超时和重试策略。单批失败被标记为 partial，但没有自动重试。

**影响**：网络波动时分类结果可能不完整，用户需要手动触发重试。

**当前状态**：已知限制。

**计划版本**：V0.4 评估（增加超时配置、重试策略、降级到规则分类）。

---

## 📋 问题统计

| 状态 | 数量 |
|------|------|
| 🔴 需要修复 | 1 |
| 🟡 已知限制 | 7 |
| 🟢 已确认延后 | 7 |
| **合计** | **15** |

> V0.3.5.1 更新：K-003（CI 未配置）和 K-015（测试服务器生命周期）已修复，数量从 3 🔴 降至 1 🔴。

---

### K-016：Content Extractor 关键词匹配准确率有限

**描述**：内容提取器使用关键词匹配推断主题，对于语义复杂或关键词不明显的文件准确率有限。例如"新建文档.txt"内容包含"财务数据"但实际主题是"项目"，关键词匹配会误判为"财务"。

**影响**：内容辅助分类可能在某些文件上产生与预期不同的结果。

**当前状态**：已知限制。V0.4.0 评估测试显示 content-aware 准确率 3/5 vs metadata-only 2/5，整体有提升。

**计划版本**：V0.5 评估（引入 LLM 语义分析替代关键词匹配）。

---

> 本文档与 `ROADMAP.md`、`DECISIONS.md` 配合使用，三份文档共同构成项目的长期知识库。
> 每轮开发后更新对应文档，确保上下文不丢失。